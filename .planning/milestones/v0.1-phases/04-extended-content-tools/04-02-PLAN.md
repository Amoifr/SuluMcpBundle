---
phase: 04-extended-content-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Tool/ArticleBlockAddTool.php
  - src/Tool/ArticleBlockRemoveTool.php
  - src/Tool/ArticleBlockReorderTool.php
  - src/Tool/ArticlePublishTool.php
  - src/Tool/ArticleUnpublishTool.php
  - tests/Unit/Tool/ArticleBlockAddToolTest.php
  - tests/Unit/Tool/ArticleBlockRemoveToolTest.php
  - tests/Unit/Tool/ArticleBlockReorderToolTest.php
  - tests/Unit/Tool/ArticlePublishToolTest.php
  - tests/Unit/Tool/ArticleUnpublishToolTest.php
autonomous: true
requirements:
  - ARTC-04

must_haves:
  truths:
    - "AI client can add a block to an article by block type"
    - "AI client can remove a block from an article"
    - "AI client can reorder blocks on an article"
    - "AI client can publish an article"
    - "AI client can unpublish an article"
  artifacts:
    - path: "src/Tool/ArticleBlockAddTool.php"
      provides: "Article block add via read-modify-dispatch"
      contains: "ModifyArticleMessage"
    - path: "src/Tool/ArticleBlockRemoveTool.php"
      provides: "Article block removal"
      contains: "ModifyArticleMessage"
    - path: "src/Tool/ArticleBlockReorderTool.php"
      provides: "Article block reorder"
      contains: "ModifyArticleMessage"
    - path: "src/Tool/ArticlePublishTool.php"
      provides: "Article publishing"
      contains: "ApplyWorkflowTransitionArticleMessage"
    - path: "src/Tool/ArticleUnpublishTool.php"
      provides: "Article unpublishing"
      contains: "ApplyWorkflowTransitionArticleMessage"
  key_links:
    - from: "src/Tool/ArticleBlockAddTool.php"
      to: "ArticleRepositoryInterface"
      via: "read current article content before modify"
      pattern: "getOneBy"
    - from: "src/Tool/ArticlePublishTool.php"
      to: "MessageBusInterface"
      via: "HandleTrait dispatch"
      pattern: "EnableFlushStamp"
---

<objective>
Article block management (add, remove, reorder) and publish/unpublish tools -- completing the article content workflow. Block tools use the read-modify-dispatch pattern with ModifyArticleMessage (per D-07). Publish tools use ApplyWorkflowTransitionArticleMessage (per D-04).

Purpose: Enable AI clients to manage article blocks and publishing lifecycle identically to pages.
Output: 5 article tool classes + 5 unit test files.
</objective>

<execution_context>
@/Users/johannes/Development/ai/sulu-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johannes/Development/ai/sulu-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-extended-content-tools/04-CONTEXT.md
@.planning/phases/04-extended-content-tools/04-RESEARCH.md

<interfaces>
<!-- Block tools mirror existing page block tools exactly, swapping Page -> Article classes -->

From src/Tool/BlockAddTool.php (reference implementation):
- Constructor: HandleTrait + MessageBusInterface $messageBus, PageRepositoryInterface, ContentManagerInterface
- $this->messageBus = $messageBus in body (not promotion)
- Pattern: read page via repository -> resolve/normalize -> extract blocks array -> splice new block -> dispatch ModifyPageMessage
- Block: array_merge(['type' => $blockType], $blockData)
- Position: null = append, integer = insert at position

From src/Tool/BlockRemoveTool.php:
- Same read pattern, then array_splice to remove by index, dispatch ModifyPageMessage

From src/Tool/BlockReorderTool.php:
- Takes $order (array of integers), reorders blocks array by index mapping, dispatch ModifyPageMessage

From src/Tool/PagePublishTool.php:
- Constructor: HandleTrait + MessageBusInterface
- Dispatches: ApplyWorkflowTransitionPageMessage(['uuid' => $uuid], $locale, 'publish')
- Returns: ['success' => true, 'uuid' => $uuid, 'action' => 'published', 'locale' => $locale]

Article-specific classes (from RESEARCH.md):
- ModifyArticleMessage(array $identifier, array $data) -- same as ModifyPageMessage
- ApplyWorkflowTransitionArticleMessage(array $identifier, string $locale, string $transitionName)
- ArticleRepositoryInterface::getOneBy(filters, selects) -- same interface as PageRepositoryInterface
- ArticleRepositoryInterface::GROUP_SELECT_ARTICLE_ADMIN
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Article block tools (add, remove, reorder) with tests</name>
  <files>
    src/Tool/ArticleBlockAddTool.php,
    src/Tool/ArticleBlockRemoveTool.php,
    src/Tool/ArticleBlockReorderTool.php,
    tests/Unit/Tool/ArticleBlockAddToolTest.php,
    tests/Unit/Tool/ArticleBlockRemoveToolTest.php,
    tests/Unit/Tool/ArticleBlockReorderToolTest.php
  </files>
  <read_first>
    src/Tool/BlockAddTool.php,
    src/Tool/BlockRemoveTool.php,
    src/Tool/BlockReorderTool.php,
    tests/Unit/Tool/BlockAddToolTest.php,
    tests/Unit/Tool/BlockRemoveToolTest.php,
    tests/Unit/Tool/BlockReorderToolTest.php
  </read_first>
  <behavior>
    - ArticleBlockAddTool: addBlock($articleUuid, $locale, $blockType, $blockProperty, $blockData, ?$position) appends/inserts block
    - ArticleBlockAddTool: dispatches ModifyArticleMessage with updated blocks array
    - ArticleBlockRemoveTool: removeBlock($articleUuid, $locale, $blockProperty, $blockIndex) removes block at index
    - ArticleBlockReorderTool: reorderBlocks($articleUuid, $locale, $blockProperty, $order) reorders blocks by index mapping
    - All three read current article via ArticleRepositoryInterface, resolve/normalize, modify blocks, dispatch ModifyArticleMessage
    - All use blockProperty parameter (per D-08)
  </behavior>
  <action>
    Create ArticleBlockAddTool.php mirroring BlockAddTool (per D-06, D-07, D-08):
    - Namespace: Sulu\McpServerBundle\Tool
    - use HandleTrait; constructor(MessageBusInterface $messageBus, ArticleRepositoryInterface $articleRepository, ContentManagerInterface $contentManager) with $this->messageBus = $messageBus in body
    - Method: addBlock(string $articleUuid, string $locale, string $blockType, string $blockProperty, array $blockData = [], ?int $position = null): array
    - #[McpTool(name: 'sulu_article_block_add', description: 'Add a content block to an article...')]
    - Read article: $this->articleRepository->getOneBy(['uuid' => $articleUuid, 'locale' => $locale, 'stage' => DimensionContentInterface::STAGE_DRAFT], [ArticleRepositoryInterface::GROUP_SELECT_ARTICLE_ADMIN => true])
    - Resolve + normalize to get current data
    - Extract blocks: $blocks = $currentData[$blockProperty] ?? []
    - Build new block: array_merge(['type' => $blockType], $blockData)
    - Insert at position or append
    - Build data array: ['locale' => $locale, 'template' => $currentData['template'] ?? null, 'title' => $currentData['title'] ?? null, $blockProperty => $blocks]
    - Dispatch: new ModifyArticleMessage(['uuid' => $articleUuid], $data) via HandleTrait
    - Return ['success' => true, 'uuid' => ..., 'blockCount' => count($blocks), 'addedAt' => $addedAt]
    - Catch \Throwable, return error with hint

    Create ArticleBlockRemoveTool.php mirroring BlockRemoveTool:
    - Same read pattern, array_splice($blocks, $blockIndex, 1), dispatch ModifyArticleMessage
    - Method: removeBlock(string $articleUuid, string $locale, string $blockProperty, int $blockIndex): array
    - #[McpTool(name: 'sulu_article_block_remove', description: 'Remove a block from an article...')]
    - Return ['success' => true, 'uuid' => ..., 'blockCount' => count($blocks), 'removedAt' => $blockIndex]

    Create ArticleBlockReorderTool.php mirroring BlockReorderTool:
    - Same read pattern, reorder blocks array by $order mapping, dispatch ModifyArticleMessage
    - Method: reorderBlocks(string $articleUuid, string $locale, string $blockProperty, array $order): array
    - #[McpTool(name: 'sulu_article_block_reorder', description: 'Reorder blocks on an article...')]
    - $order is array of integers representing new index positions

    Create unit tests for all three following existing BlockAddToolTest pattern:
    - Mock ArticleRepositoryInterface, ContentManagerInterface, MessageBusInterface
    - Use ArticleInterface mock for repository return
    - Test block manipulation logic, dispatch verification, error handling, MCP attribute
  </action>
  <verify>
    <automated>cd /Users/johannes/Development/ai/sulu-mcp && composer test -- --filter "ArticleBlock"</automated>
  </verify>
  <acceptance_criteria>
    - src/Tool/ArticleBlockAddTool.php contains "#[McpTool(name: 'sulu_article_block_add'"
    - src/Tool/ArticleBlockAddTool.php contains "ModifyArticleMessage" (not ModifyPageMessage)
    - src/Tool/ArticleBlockAddTool.php contains "ArticleRepositoryInterface"
    - src/Tool/ArticleBlockAddTool.php contains "$blockProperty" parameter
    - src/Tool/ArticleBlockRemoveTool.php contains "#[McpTool(name: 'sulu_article_block_remove'"
    - src/Tool/ArticleBlockReorderTool.php contains "#[McpTool(name: 'sulu_article_block_reorder'"
    - All three files contain "HandleTrait"
    - composer test -- --filter "ArticleBlock" exits 0
  </acceptance_criteria>
  <done>Three article block tools exist with tests, all using read-modify-dispatch pattern with ModifyArticleMessage and blockProperty parameter</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Article publish/unpublish tools with tests</name>
  <files>
    src/Tool/ArticlePublishTool.php,
    src/Tool/ArticleUnpublishTool.php,
    tests/Unit/Tool/ArticlePublishToolTest.php,
    tests/Unit/Tool/ArticleUnpublishToolTest.php
  </files>
  <read_first>
    src/Tool/PagePublishTool.php,
    src/Tool/PageUnpublishTool.php,
    tests/Unit/Tool/PagePublishToolTest.php,
    tests/Unit/Tool/PageUnpublishToolTest.php
  </read_first>
  <behavior>
    - ArticlePublishTool: publishArticle($uuid, $locale) dispatches ApplyWorkflowTransitionArticleMessage with 'publish'
    - ArticlePublishTool: returns ['success' => true, 'uuid' => $uuid, 'action' => 'published', 'locale' => $locale]
    - ArticleUnpublishTool: unpublishArticle($uuid, $locale) dispatches same message with 'unpublish'
    - Both tools include confirmation warning in description (per page tool pattern)
  </behavior>
  <action>
    Create ArticlePublishTool.php mirroring PagePublishTool exactly (per D-04):
    - use HandleTrait; constructor(MessageBusInterface $messageBus) with $this->messageBus = $messageBus in body
    - Method: publishArticle(string $uuid, string $locale): array
    - #[McpTool(name: 'sulu_article_publish', description: 'Publish an article to make it visible... IMPORTANT: Always ask the user for confirmation before calling this tool.')]
    - Dispatch: new ApplyWorkflowTransitionArticleMessage(['uuid' => $uuid], $locale, 'publish')
    - Use Sulu\Article\Application\Message\ApplyWorkflowTransitionArticleMessage
    - Wrap in Envelope with EnableFlushStamp
    - Return ['success' => true, 'uuid' => $uuid, 'action' => 'published', 'locale' => $locale]
    - Catch \Throwable, return ['error' => sprintf('Failed to publish article %s: %s', $uuid, $e->getMessage()), 'hint' => ...]

    Create ArticleUnpublishTool.php mirroring PageUnpublishTool:
    - Same pattern but transition = 'unpublish'
    - #[McpTool(name: 'sulu_article_unpublish', description: 'Unpublish a live article...')]
    - Return ['success' => true, 'uuid' => $uuid, 'action' => 'unpublished', 'locale' => $locale]

    Create unit tests following PagePublishToolTest pattern:
    - Mock MessageBusInterface, verify dispatch with correct message class
    - Test success response, error handling, MCP attribute
  </action>
  <verify>
    <automated>cd /Users/johannes/Development/ai/sulu-mcp && composer test -- --filter "ArticlePublishToolTest|ArticleUnpublishToolTest"</automated>
  </verify>
  <acceptance_criteria>
    - src/Tool/ArticlePublishTool.php contains "ApplyWorkflowTransitionArticleMessage" (not Page variant)
    - src/Tool/ArticlePublishTool.php contains "'publish'" transition string
    - src/Tool/ArticlePublishTool.php contains "#[McpTool(name: 'sulu_article_publish'"
    - src/Tool/ArticleUnpublishTool.php contains "'unpublish'" transition string
    - src/Tool/ArticleUnpublishTool.php contains "#[McpTool(name: 'sulu_article_unpublish'"
    - Both files contain "HandleTrait" and "EnableFlushStamp"
    - composer test -- --filter "ArticlePublishToolTest|ArticleUnpublishToolTest" exits 0
  </acceptance_criteria>
  <done>ArticlePublishTool and ArticleUnpublishTool exist with tests, dispatch ApplyWorkflowTransitionArticleMessage with correct transitions</done>
</task>

</tasks>

<verification>
composer test -- --filter "ArticleBlock|ArticlePublish|ArticleUnpublish" passes all tests
composer phpstan passes with no new errors
All 5 tools have correct #[McpTool] attributes with sulu_article_* names
</verification>

<success_criteria>
- 5 article block + publish tool files exist in src/Tool/
- 5 corresponding test files exist in tests/Unit/Tool/
- All tests pass via composer test
- PHPStan passes
- Block tools use ModifyArticleMessage (not ModifyPageMessage)
- Publish tools use ApplyWorkflowTransitionArticleMessage (not Page variant)
</success_criteria>

<output>
After completion, create `.planning/phases/04-extended-content-tools/04-02-SUMMARY.md`
</output>
